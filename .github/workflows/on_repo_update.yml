name: Merge from repos on dispatch

on:
  repository_dispatch:
    types: [fork-update]

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.CR_PAT }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add fork remote dynamically
        run: |
          FORK_OWNER=$(jq -r .fork <<< '${{ toJson(github.event.client_payload) }}')
          REPO_NAME=$(jq -r .repo <<< '${{ toJson(github.event.client_payload) }}')
          echo "FORK_OWNER=$FORK_OWNER"
          echo "REPO_NAME=$REPO_NAME"
          git remote add "${REPO_NAME}" https://github.com/${FORK_OWNER}/${REPO_NAME}.git

      - name: Fetch & merge from fork
        run: |
          git fetch "${REPO_NAME}" main || { echo "Failed to fetch main branch"; exit 1; }
          git merge --no-ff "${REPO_NAME}" -m "Auto-merge from ${FORK_OWNER}/${REPO_NAME}"

      - name: Push changes
        run: git push origin main
